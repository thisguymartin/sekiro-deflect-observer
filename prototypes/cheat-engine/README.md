# Cheat Engine research prototype

The handover describes an existing V0.1 prototype with embedded Lua, a read-only memory observer, an external timing-history window, and synthetic-memory tests. Those files are not included here yet. This directory preserves the intended location for the original research.

## Import the prototype

1. Copy the original table, Lua sources, and tests into this directory without editing their contents.
2. Record the original source, author, revision, and SHA-256 hashes.
3. Record any third-party code and its license before publishing the files.
4. Inspect the startup and shutdown paths before running the table.
5. Replace the missing entries below with instructions verified against those files.
6. Follow [Test the research prototype](../../docs/testing.md).

## Required operating instructions

| Item | Current status |
|---|---|
| Table filename and original revision | Missing |
| Companion Lua files and load paths | Missing |
| Required Cheat Engine version | Unverified |
| Recognized executable identity and offset provenance | Unverified; the handover targets PC 1.06 |
| Exact activation entry or script | Missing |
| Exact stop action and timer cleanup | Missing |
| Display labels and their meaning | Unverified |
| Poll interval and stale-state timeout | Unverified |
| History clock and duration calculation | Unverified |
| Synthetic test prerequisites, command, and expected output | Missing |

F8, in-game rendering, settings, automatic reconnection, and automatic build detection are proposed features. Do not assume that this prototype implements them.
